---
import { ColorsEnum } from '../../constants/colors';

interface Props
{
    id   : string;
    color: ColorsEnum;
}

const { id, color } = Astro.props;

---

<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke={color} id={id} class="h-full w-full absolute invisible">
    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
</svg>